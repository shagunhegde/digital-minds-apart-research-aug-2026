# Every artifact is a file on disk, but they are big and regenerable, and the
# sweep shards alone are ~350 MB. Mirror them to an HF dataset instead:
#   huggingface-cli upload <you>/introspection-factorization artifacts --repo-type=dataset
artifacts/
RESULTS.md          # generated by scripts/05_figures.py from artifacts

# fetched at run time from pinned commits, never edited here
vendor/

.venv/
__pycache__/
*.pyc
*.pt
.DS_Store
.claude/settings.local.json
