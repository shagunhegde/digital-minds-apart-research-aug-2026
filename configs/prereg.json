{
 "note": "The build spec is the preregistration document: every analysis it names was specified before any data existed. Anything added during the build is exploratory, including additions that were forced by an asset turning out to be missing. Listed honestly in both directions, per G5's requirement.",
 "prereg_document": "Claude Code Build Spec: Introspection Factorization",
 "specified": [
  {"id": "cascade", "what": "P(report) = f1 x f2 x f3, with f1 a cross-validated mass-mean probe at 5% FPR on control trials, f2 the concept token inside top-k of the J-lens readout at the report position, f3 the judge scoring the generation as naming the concept", "gate": "G4"},
  {"id": "cascade_residual", "what": "|f1*f2*f3 - observed| reported as a raw number at every k and in both orders", "gate": "G4"},
  {"id": "survivorship", "what": "counts entering f1 and surviving each stage", "gate": "G4"},
  {"id": "probe_null", "what": "f1 recomputed with shuffled labels", "gate": "G4"},
  {"id": "f2_null_band", "what": "matched-norm random vectors through the identical pipeline; f2 thresholds as a quantile of this rather than an absolute value", "gate": "G4"},
  {"id": "position_control", "what": "f2 at the report, injection and a random position", "gate": "G4"},
  {"id": "k_curve", "what": "f2 over k in {1,5,10,25,50} with the null band overlaid", "gate": "G4"},
  {"id": "layer_sensitivity", "what": "f2 at each of the three readout layers", "gate": "G4"},
  {"id": "jackknife", "what": "leave-one-concept-out range on each factor", "gate": "G4"},
  {"id": "judge_noise", "what": "f3 recomputed with judge disagreements flipped, as an interval", "gate": "G4"},
  {"id": "r_lens", "what": "same pipeline under the R-lens if available", "gate": "G4"},
  {"id": "tpr_fpr_grid", "what": "TPR and FPR with Wilson intervals per (layer, strength), TPR-FPR as the discriminability scalar, AUC across the strength ladder", "gate": "G3"},
  {"id": "yes_bias", "what": "the Godet confound: same vectors injected into unrelated yes/no questions whose default answer is no", "gate": "G3"},
  {"id": "bimodality", "what": "Hartigan dip test on the per-concept detection distribution with a p-value", "gate": "G3"},
  {"id": "cross_model_spearman", "what": "Spearman between our per-concept rates and Macar's Gemma rates, with CI", "gate": "G3", "status": "NOT COMPUTED -- the per-concept Gemma rates are not published anywhere reachable; see ASSETS.md section 12"},
  {"id": "confabulation", "what": "top-10 most frequent wrong identifications with counts", "gate": "G3"},
  {"id": "composition_audit", "what": "abstract/concrete split before and after the single-token filter", "gate": "G3"},
  {"id": "judge_kappa", "what": "Cohen's kappa against 50 hand-labelled responses plus the confusion matrix", "gate": "G3", "status": "PENDING -- requires human labels; G3 emits the sample and computes kappa once they exist"},
  {"id": "zero_strength_identity", "what": "alpha=0 versus no hook at all, max absolute logit difference", "gate": "G2"},
  {"id": "batch_equivalence", "what": "B=4 batched versus 4 singles, reported as a raw number", "gate": "G2"},
  {"id": "spatial_containment", "what": "max absolute residual difference outside the injection window at the injection layer and every layer below", "gate": "G2"},
  {"id": "dose_response", "what": "target token probability versus alpha, Spearman per concept", "gate": "G2"},
  {"id": "steering_specificity", "what": "confusion matrix on a 10-concept subset", "gate": "G2"},
  {"id": "coherence_onset", "what": "perplexity of generated text versus clean, per alpha", "gate": "G2"},
  {"id": "three_way_lens", "what": "J-lens, logit lens and a random-rotation null lens compared on rank, not only hit rate", "gate": "G1"},
  {"id": "shuffled_position_null", "what": "reading the target at randomly chosen positions", "gate": "G1"},
  {"id": "random_token_calibration", "what": "KS against uniform for prompt-irrelevant tokens", "gate": "G1"},
  {"id": "final_layer_convergence", "what": "J-lens and logit lens agreement rising toward the last fitted layer", "gate": "G1"},
  {"id": "depth_ordering", "what": "intermediate entity surfacing earlier in depth than the final answer", "gate": "G1"},
  {"id": "trash_token_rate", "what": "fraction of top-10 readouts that are special tokens, byte fragments or non-words", "gate": "G1"},
  {"id": "temporal_drift", "what": "a control statistic compared between the first and last decile of shards", "gate": "G4a"},
  {"id": "fpr_matched", "what": "cross-condition claims restated at a common FPR", "gate": "G5"},
  {"id": "multiplicity", "what": "number of comparisons across the condition grid and BH-adjusted q-values for headline claims", "gate": "G5"},
  {"id": "power", "what": "minimum detectable effect at 80% power for any null being claimed", "gate": "G5"},
  {"id": "order_contrast_ci", "what": "confidence interval on the difference between orders", "gate": "G5"}
 ],
 "exploratory": [
  {"id": "own_pilot_stratification", "what": "concepts stratified on detection measured by this project over the full single-token pool, rather than on Macar's Gemma rates", "why": "the specified rates are not published; see ASSETS.md section 12. Avoids cross-model regression to the mean, but forfeits the cross-model Spearman."},
  {"id": "dip_on_full_pool", "what": "the bimodality dip is computed on the whole single-token pool, not on the selected 60", "why": "the 60 were selected to span the detection range, which would manufacture the spread. The selected-60 dip is also reported, explicitly labelled selection-induced."},
  {"id": "constructed_report_position", "what": "the assistant turn is prefilled up to the detection key so the next token is a JSON boolean in both orders", "why": "the report position is only well defined for report-first otherwise; reading both orders at the last prompt token would compare a detection verdict against a task answer and call the difference an order effect."},
  {"id": "zero_strength_dedupe", "what": "control_zero cells are computed once per (batch, layer, order) and broadcast across strengths", "why": "alpha=0 adds exactly zero, which G2 measures, so this is exact rather than an approximation. Kept per-batch so the drift probe still has a per-batch statistic."},
  {"id": "cached_vs_recomputed_rank", "what": "the report-position rank is computed both from the sweep cache and from a fresh forward, and the agreement reported", "why": "catches the sweep and the factor pass having built different prompts."},
  {"id": "naive_vs_cascade_rate", "what": "the end-to-end report rate is reported both cascade-consistently and naively, with the gap", "why": "the gap is trials that name the concept without clearing the probe or the readout -- leakage in the operationalisation rather than a property of the model."},
  {"id": "mixed_bucket", "what": "the composition audit uses three buckets (concrete / abstract / mixed) rather than a forced binary", "why": "professions, science terms, sports, music/art, weather and actions-as-nouns are neither cleanly; forcing them would invent a split."},
  {"id": "cluster_bootstrap", "what": "every interval on a factor is a bootstrap over concepts, not over trials", "why": "f1 and f2 are shared across the samples drawn from one prefill residual, so trials are not independent."},
  {"id": "second_jlens_arm", "what": "a second independent J-lens fit as a robustness arm", "why": "camilablank/workspace-lenses hosts a different fit of the same object; bounds how much of f2 is lens-fit noise."},
  {"id": "word_and_item_pass_at_k", "what": "G1 reports both the published item-level fraction and a word-level proportion", "why": "the published metric is a mean of per-item fractions, which is not a raw proportion, so Wilson does not apply to it."},
  {"id": "deterministic_identification", "what": "identification scored by word-boundary match accepting singular forms, with the LLM judge optional and explicitly flagged when unavailable", "why": "the concept list is plural; a model naming 'trumpet' for 'Trumpets' is a correct identification. Keeps the pipeline runnable without an API key, but 'the judge said so' and 'a regex said so' are different claims and are never conflated."}
 ]
}
